echo "instancias de ej3" > hola.output
echo "" >> hola.output
for i in ../ej3/instancias-3/instancias/*.txt
do
	base_name=$(basename $i)
	echo ${base_name} >> hola.output
	/usr/bin/time -f "Tardó: %e" -o hola.output -a timeout 800 ./tp1 3 < $i >> hola.output
	echo "" >> hola.output
done
